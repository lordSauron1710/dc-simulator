# API Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository API policy at the expected path.

The canonical API policy lives at [docs/policies/API.md](docs/policies/API.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
