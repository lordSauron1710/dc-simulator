# Security Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository security policy at the expected path.

The canonical security policy lives at [docs/policies/SECURITY.md](docs/policies/SECURITY.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
