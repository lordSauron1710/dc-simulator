# Deployment Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository deployment policy at the expected path.

The canonical deployment policy lives at [docs/policies/DEPLOYMENT.md](docs/policies/DEPLOYMENT.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
