# Database Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository database policy at the expected path.

The canonical database policy lives at [docs/policies/DATABASE.md](docs/policies/DATABASE.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
