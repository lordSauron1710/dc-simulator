# Auth Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository auth policy at the expected path.

The canonical auth policy lives at [docs/policies/AUTH.md](docs/policies/AUTH.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
