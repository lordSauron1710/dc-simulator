# Environment Variables Policy Compatibility Stub

This file exists so root-only tooling such as `npx secure-repo audit` can find the repository environment-variable policy at the expected path.

The canonical environment-variable policy lives at [docs/policies/ENV_VARIABLES.md](docs/policies/ENV_VARIABLES.md).

Follow the canonical document for all repo work. If this stub and the canonical policy ever diverge, the canonical policy wins.
